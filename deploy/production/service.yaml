apiVersion: v1
kind: Service
metadata:
  labels:
    app: mcp-nanobanana
  name: mcp-nanobanana
  namespace: acedatacloud
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: mcp-nanobanana
